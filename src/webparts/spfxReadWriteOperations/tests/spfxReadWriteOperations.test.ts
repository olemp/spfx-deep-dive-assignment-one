/// <reference types="jest" />  
  
describe('SpfxReadWriteOperations tests', () => {  
  
  beforeEach(() => {  
  
  });  
  
  afterEach(() => {  
   
  });  
  
  
  it('should return the correct result', () => {  
    // Assert  
    expect(1+1).toBe(2);    
  });  

  it('should return the correct result 2', () => {  
    // Assert  
    expect(1-1).toBe(0);    
  }); 
});